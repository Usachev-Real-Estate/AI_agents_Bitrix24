services:
  auditor:
    build: .
    container_name: b24-ai-auditor
    restart: "no"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    command: ["python", "src/main.py"]
