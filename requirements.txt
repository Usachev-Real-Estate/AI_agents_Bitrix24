# Верхние границы защищают от неожиданного мажорного апгрейда при пересборке
# образа. Для полностью воспроизводимого деплоя сгенерируйте лок-файл:
#   pip install pip-tools && pip-compile requirements.txt -o requirements.lock
# и ставьте из него: pip install -r requirements.lock
langgraph>=0.2.0,<1.0
langchain-openai>=0.2.0,<1.0
fast-bitrix24>=1.0.0,<2.0
python-dotenv>=1.0.0,<2.0
pydantic-settings>=2.0.0,<3.0
# используется напрямую в exclusive_expiry.py и notify.py
httpx>=0.27.0,<1.0

# --- Доставка выгрузки досье в Google Drive (src/dossier_drive.py) ---
# Нужны только для доставки. Без них сбор досье работает и складывает файлы
# в data/dossier — отсюда ленивый импорт в модуле, а не отсюда.
google-api-python-client>=2.120.0,<3.0
google-auth>=2.28.0,<3.0

# --- Веб-дашборд аналитики (src/web) ---
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
jinja2>=3.1.0,<4.0
# разбор данных формы логина
python-multipart>=0.0.9,<1.0
# подпись куки сессии
itsdangerous>=2.1.0,<3.0
# argon2id для паролей
argon2-cffi>=23.1.0,<26.0
